// Giants Engine - UI Text Layout
// Reverse engineered from GiantsMain.exe v1.520.59
#pragma once

#include <cstdint>

namespace Giants {

// ─── LayoutUIText (FUN_005da8f0) ── PASS ──
// Renders or lays out UI text with timing, string length measurement,
// and interpolation. Returns 0xFFFFFF on completion.
// 23 callers, 3 callees.
uint32_t LayoutUIText(int param_1, int param_2, int param_3, int param_4);  // FUN_005da8f0

} // namespace Giants
